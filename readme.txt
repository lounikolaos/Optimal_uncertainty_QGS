I now changed the filter function q(nu) to a gaussian. Now the beta behavior is much better. 

The code from my BA (files: Gibbs_sampler_notebook_BA.ipynb, gibbs_sampler_BA.py) is now also uploaded. 
